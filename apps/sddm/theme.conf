[General]
# What the theme offers through `config.*` inside Main.qml.
type=color
color=#0f1115
# Optional: an absolute path to an image. Empty falls back to the colour
# above -- the greeter cannot read the user's Plasma wallpaper, so the
# picture has to be named here.
background=
showClock=true
