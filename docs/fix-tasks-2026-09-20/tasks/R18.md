# R18 · settings 死代码 / 调用不存在 bridge 方法清理

| 字段 | 值 |
|---|---|
| 阶段 | 三 |
| 并发组 | H（依赖 R17，同 main.qml 串行） |
| 依赖 | R17 |
| 优先级 | P1 |
| 分支 | `fix/2026-09-20-settings-deadcode` |

## 问题
- `apps/settings/main.qml:561/572/583/602/621` 调 `bridge.updateDockBlurInherit/updateDockBlurStrength/updateDockLiquidStrength`；`:3140/3151/3162/3181/3200` 调 `updateBarBlurInherit/updateBarBlurStrength/updateBarLiquidStrength`——**main.cpp 全都没有**（只有 `updateGlobal*`/`updateDockIconOpacity`），触发即 TypeError。这些块全在 `visible:false` 死 UI 里（`:1049/1059/1308/1314/3350/3356`），约 400 行。
- `main.qml:3` `import QtQuick.Effects` 零使用（死 import）。
- `DockSettingsPage`(506) 与 `IconAppearanceSection`(2139) 的 hue/tone/preset/colorHex/colorFromHex/blend/toneColor/hueForColor/nearestTone 逐字两套；`BarSettingsPage`/`DockSettingsPage` 的 preview/commit debounce 逐字一套。
- `main.qml` 4367 行单文件，维护风险（拆分建议见 R29，本任务不强拆）。

## 改动点
- 二选一：实现缺失的 bridge 方法并放开 UI，或删除死代码块。
- 删 `import QtQuick.Effects` 死 import。
- 抽取重复的 hue/tone/debounce 工具到共享文件（可选，不夹带大重构）。

## 验证
- `qmllint` 无 TypeError 引用；settings 打开正常。
- 若保留 UI：dock/bar blur/liquid 滑条实际生效。

## 审查要点
- 删除的死代码没有被活路径引用。
- 若实现方法，bridge 方法签名与 QML 调用一致。

## 完成定义（强制）

- ☐ 实现 + 验证通过后，**必须**由本代理把 `git diff` 交给只读 `code-reviewer` 子代理审查，
  通过才算完成；未通过 → 修复后重新送审。`fixer` 自身验证不算完成。
