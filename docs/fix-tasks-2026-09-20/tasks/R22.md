# R22 · weather `WeatherClient` fallback 轮询门控 + buffer 上限

| 字段 | 值 |
|---|---|
| 阶段 | 三 |
| 并发组 | K（独立文件） |
| 依赖 | 无 |
| 优先级 | P1 |
| 分支 | `fix/2026-09-20-weather-pollgate` |

## 问题
`apps/weather/src/WeatherClient.cpp:79-84` — `m_snapshotFallbackTimer` 每 10s `reloadSnapshot()`（同步读文件+JSON 解析），**socket 已连接时也照跑**；`QFileSystemWatcher` 已覆盖变更通知，纯浪费。`:293-303` `readSocketLines` 的 `m_readBuffer` 无上限——对端不发 `\n` 会无限涨内存。`:357` `m_pendingRequests` cap 16 是对的（保留）。

## 改动点
- `m_snapshotFallbackTimer` 仅 `!connected()` 时运行（connectedChanged 门控），或增大到 60s+且按 mtime 跳过相同文件。
- `m_readBuffer` 加上限（如 1MB），超限 abort/重连并记日志。

## 验证
- socket 已连时不再每 10s 读文件（日志/strace 对比）。
- 对端发送无换行长数据时 buffer 不无限涨。

## 审查要点
- fallback 停止后断线能重新激活。
- buffer 上限触发后连接恢复逻辑正确。

## 完成定义（强制）

- ☐ 实现 + 验证通过后，**必须**由本代理把 `git diff` 交给只读 `code-reviewer` 子代理审查，
  通过才算完成；未通过 → 修复后重新送审。`fixer` 自身验证不算完成。
