# R15 · daemon `clipboard.history.list` 加缓存/TTL

| 字段 | 值 |
|---|---|
| 阶段 | 二 |
| 并发组 | D（依赖 R10，同 PlatformServer.cpp 串行） |
| 依赖 | R10 |
| 优先级 | P1 |
| 分支 | `fix/2026-09-20-daemon-clipboard-cache` |

## 问题
`PlatformServer.cpp:2308-2319` — `clipboard.history.list` 每次都跑 `cliphist list` 全量 fork+exec + `clipboardPruneThumbs` 全目录遍历 + 全量 stdout 解析。面板每次刷新都触发；history 条目多时开销可观。无前台缓存/TTL。

## 改动点
- 给 history 列表加 TTL 缓存（如 1-2s）或基于 cliphist 变更信号失效。
- `clipboardPruneThumbs` 降频或仅在 history 变化时跑。
- 与 R6 超时/R9 读缓冲改动协调（同文件）。

## 验证
- 面板连续刷新不再每次都 fork+exec cliphist。
- 复制新条目后缓存能及时失效（数据仍新鲜）。

## 审查要点
- 缓存失效时机（复制后应立即反映）。
- 缩略图 prune 是否漏清孤儿文件。

## 完成定义（强制）

- ☐ 实现 + 验证通过后，**必须**由本代理把 `git diff` 交给只读 `code-reviewer` 子代理审查，
  通过才算完成；未通过 → 修复后重新送审。`fixer` 自身验证不算完成。
