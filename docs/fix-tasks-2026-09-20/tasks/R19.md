# R19 · calendar `itemsForDate` 预建 map（消 168× 全扫）

| 字段 | 值 |
|---|---|
| 阶段 | 三 |
| 并发组 | I（独立文件树） |
| 依赖 | 无 |
| 优先级 | P1 |
| 分支 | `fix/2026-09-20-calendar-itemmap` |

## 问题
`apps/calendar/qml/CalendarScheduleView.qml:236-245` — `Repeater model: 24*(dayCount+1)`（周视图 168 cell），每 cell `cellItems` 调 `root.hourItems(date,hour)` → `itemsForDate()`（`Main.qml:218-258`）对 `pim.occurrences`+`pim.todoOccurrences` **全扫描 + 构造新数组 + sort**。`searchQuery`/`selectedDate`/`now`/`occurrencesChanged` 任一变化 → 168 次 O(n) 扫描 + 168 次数组分配。`usefulStartHour()`(`:56-78`) 又每 positionTimeline 加 dayCount 次扫。`CalendarMonthView.qml:76` 同模式 ×42。

## 改动点
- `occurrencesChanged` 时一次构建 `Map<dateKey, items>`，hour cell 只 `map.get(key)` 过滤 hour。
- `usefulStartHour` 用预建数据而非重复扫描。
- month view 复用同一 map。

## 验证
- 周视图滚动/切日无卡顿（对比刷新耗时）。
- 日程/待办显示正确（按日归组无遗漏）。

## 审查要点
- map 重建时机（occurrences/searchQuery 变化）覆盖所有失效路径。
- 日期 key 与时区/跨日 occurrence 处理一致。

## 完成定义（强制）

- ☐ 实现 + 验证通过后，**必须**由本代理把 `git diff` 交给只读 `code-reviewer` 子代理审查，
  通过才算完成；未通过 → 修复后重新送审。`fixer` 自身验证不算完成。
